import SearchBar from "../SearchBar";
import Product from "../Product";

const RightSide = () => {
  return (
    <div className="col-6">
      <SearchBar />
      <Product />
    </div>
  );
};

export default RightSide;
